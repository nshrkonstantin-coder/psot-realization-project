# 📧 Настройка системы отправки Email

Эта документация описывает как настроить и использовать систему отправки email-уведомлений в АСУБТ.

## 🎯 Возможности

- ✅ Реальная отправка email через SMTP
- ✅ Детальная отчетность о каждой отправке
- ✅ Валидация email адресов
- ✅ Отслеживание существования адресов
- ✅ Диагностика проблем подключения
- ✅ Красивые HTML-письма
- ✅ Тестовая страница для проверки

## 📋 Требуемые настройки

В секретах проекта должны быть настроены следующие переменные:

### 1. SMTP_HOST
**Описание**: Адрес SMTP сервера

**Примеры**:
- Yandex: `smtp.yandex.ru`
- Gmail: `smtp.gmail.com`
- Mail.ru: `smtp.mail.ru`
- Outlook: `smtp-mail.outlook.com`

### 2. SMTP_PORT
**Описание**: Порт SMTP сервера

**Значения**:
- `587` - для STARTTLS (рекомендуется)
- `465` - для SSL/TLS
- `25` - незащищенный (не рекомендуется)

### 3. SMTP_USER
**Описание**: Email для авторизации на SMTP сервере

**Пример**: `your-email@yandex.ru`

### 4. SMTP_PASSWORD
**Описание**: Пароль от почты или пароль приложения

⚠️ **Важно**: 
- Для Gmail используйте "Пароль приложения" (App Password)
- Для Yandex можно использовать обычный пароль
- Для корпоративной почты уточните у администратора

## 🔧 Настройка для популярных сервисов

### Yandex Mail

```
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-password
```

**Примечания**:
- Можно использовать обычный пароль
- Рекомендуется включить двухфакторную аутентификацию

### Gmail

```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=app-password-here
```

**Настройка пароля приложения**:
1. Перейдите в настройки аккаунта Google
2. Безопасность → Двухэтапная аутентификация
3. Пароли приложений → Создать новый
4. Выберите "Почта" и "Другое устройство"
5. Скопируйте сгенерированный пароль (16 символов)

### Mail.ru

```
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
SMTP_USER=your-email@mail.ru
SMTP_PASSWORD=your-password
```

### Outlook/Hotmail

```
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=your-email@outlook.com
SMTP_PASSWORD=your-password
```

## 🧪 Тестирование

### Тестовая страница

Откройте `/email-test` для доступа к специальной странице тестирования:

1. Введите тестовые email адреса (можно несколько через запятую)
2. Настройте тему и текст письма
3. Нажмите "Отправить тестовое письмо"
4. Получите детальный отчет о статусе каждой отправки

### Использование в коде

```typescript
import { useEmailSender } from '@/hooks/useEmailSender';

const { sendEmail, sending, lastResult } = useEmailSender();

const handleSend = async () => {
  const result = await sendEmail({
    recipients: ['user1@example.com', 'user2@example.com'],
    subject: 'Тема письма',
    html_content: '<h1>Привет!</h1><p>Это тестовое письмо</p>',
    sender_name: 'АСУБТ'
  });
  
  if (result) {
    console.log(`Отправлено: ${result.sent}`);
    console.log(`Не отправлено: ${result.failed}`);
    console.log('Детали:', result.results);
  }
};
```

## 📊 Формат ответа

Backend функция возвращает детальную информацию:

```json
{
  "success": true,
  "total": 3,
  "sent": 2,
  "failed": 1,
  "results": [
    {
      "email": "valid@example.com",
      "success": true,
      "message": "Письмо успешно отправлено",
      "valid_format": true
    },
    {
      "email": "invalid-email",
      "success": false,
      "message": "Неверный формат email адреса",
      "valid_format": false
    }
  ],
  "summary": {
    "sent_to": ["valid@example.com"],
    "failed_to": [
      {
        "email": "invalid-email",
        "reason": "Неверный формат email адреса"
      }
    ]
  }
}
```

## 🐛 Частые проблемы и решения

### Ошибка: "Ошибка аутентификации SMTP"

**Причина**: Неверные SMTP_USER или SMTP_PASSWORD

**Решение**:
- Проверьте правильность написания email
- Для Gmail используйте пароль приложения (App Password)
- Убедитесь что не копируете лишние пробелы

### Ошибка: "Email отклонен сервером"

**Причина**: Email адрес получателя не существует

**Решение**:
- Проверьте правильность написания адреса
- Убедитесь что домен существует
- Проверьте что адрес активен

### Ошибка: "Не удалось подключиться к SMTP серверу"

**Причина**: Неверный SMTP_HOST или SMTP_PORT

**Решение**:
- Проверьте адрес SMTP сервера
- Убедитесь что используете правильный порт (587 или 465)
- Проверьте что сервер доступен (не заблокирован файрволом)

### Ошибка: "Неверный формат email адреса"

**Причина**: Email не соответствует стандартному формату

**Решение**:
- Формат должен быть: `name@domain.com`
- Проверьте наличие символа `@`
- Убедитесь что есть доменная часть после `@`

## 📁 Структура файлов

```
backend/send-email/
├── index.py              # Основная функция отправки
├── requirements.txt      # Зависимости (email-validator)
└── tests.json           # Тесты

src/
├── hooks/
│   └── useEmailSender.ts        # React хук для отправки
├── components/
│   └── EmailStatusDialog.tsx    # Диалог с результатами
└── pages/
    ├── EmailTestPage.tsx        # Страница тестирования
    └── PabRegistrationPage.tsx  # Использование в ПАБ
```

## 🔐 Безопасность

- ✅ Все SMTP настройки хранятся в секретах проекта
- ✅ Пароли никогда не передаются на фронтенд
- ✅ Backend функция изолирована в Cloud Function
- ✅ Поддержка STARTTLS шифрования
- ⚠️ Не логируйте пароли и email в консоль

## 📝 API Backend функции

### URL
`https://functions.poehali.dev/ca9e0986-48d7-46a1-b0be-7a98ddf4c429`

### Метод
`POST`

### Тело запроса
```json
{
  "recipients": ["email1@example.com", "email2@example.com"],
  "subject": "Тема письма",
  "html_content": "<h1>HTML содержимое</h1>",
  "sender_name": "Имя отправителя (опционально)"
}
```

### Заголовки
```
Content-Type: application/json
```

### Коды ответа
- `200` - Успешная обработка (проверьте results для деталей)
- `400` - Неверные параметры запроса
- `500` - Ошибка SMTP конфигурации или подключения

## 🎨 Кастомизация HTML писем

Письма поддерживают полный HTML и CSS:

```typescript
const htmlContent = `
  <div style="font-family: Arial, sans-serif; max-width: 600px;">
    <div style="background: #1e40af; color: white; padding: 20px;">
      <h1>Заголовок письма</h1>
    </div>
    <div style="padding: 20px;">
      <p>Ваш текст...</p>
      <a href="https://example.com" style="color: #1e40af;">
        Ссылка
      </a>
    </div>
  </div>
`;
```

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте настройки SMTP в секретах
2. Используйте тестовую страницу `/email-test`
3. Проверьте логи backend функции
4. Убедитесь что почтовый сервер доступен
5. Проверьте лимиты на отправку у вашего провайдера

---

**Версия**: 1.0  
**Дата обновления**: 2024-12-09
